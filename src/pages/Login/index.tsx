import React from 'react';
import { Card } from './Card';

import * as Styled from './styled';

export const Login = () => {
  return (
    <Styled.Container>
      <Card />
    </Styled.Container>
  );
};
